import React from 'react';
import { NativeRouter, Switch, Route, Link } from 'react-router-native';

export default () => (
  <NativeRouter>
    <Switch>
      <Route />
    </Switch>
  </NativeRouter>
);
